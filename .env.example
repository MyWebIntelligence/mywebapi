# =============================================================================
# MyWebIntelligence - Configuration Docker Compose
# Copiez ce fichier en .env et ajustez les valeurs
# =============================================================================

# Port du client web (accessible sur http://localhost:3000)
MWI_CLIENT_PORT=3000

# Celery autoscale (min,max workers) - laisser vide pour desactiver
CELERY_AUTOSCALE=4,12
